# cp this to .env.local. This is the Turnstile *site key* (public, safe in the client bundle) —
# the secret key lives in Cloudflare Functions env vars, see .dev.vars.example.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
