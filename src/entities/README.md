# entities

도메인 객체와 그 표현(모델 + API + UI).

- `activity/`: 활동 목록 카드, 신청 인트로 카드, 목록 조회 API
- `executive/`: 임원진 카드, 팀 카드, 로스터 카드, 목록 조회 API

같은 도메인 객체를 여러 화면에서 다르게 보여줄 때(목록 카드 vs 상세 카드 등) 그 UI들을
한 슬라이스에 모아둔다. 데이터 출처(더미 vs 실제 API)가 바뀌어도 `api/` 안쪽만 교체하면 되도록
`ui/`·`model/`은 반환 타입에만 의존한다.
